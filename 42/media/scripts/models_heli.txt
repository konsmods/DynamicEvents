module Base
{
    -- Register the 3D model. The mesh path is relative to media/models/
    -- PZ B42 supports .fbx directly (no .x conversion needed in most cases)
    model HelicopterWreck
    {
        mesh = helicopter_wreck,
        shader = static,
        scale = 1.0,
    }
}
