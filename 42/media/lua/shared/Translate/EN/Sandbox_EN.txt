Sandbox_EN = {
    Sandbox_DynamicEvents = "Dynamic Events",

    Sandbox_DynamicEvents_Enabled = "Enable Dynamic Events",
    Sandbox_DynamicEvents_Enabled_tooltip = "Master switch for spawning new events. Events already in the world are unaffected and stay until an admin clears them.",

    Sandbox_DynamicEvents_MinHoursBetweenEvents = "Hours between events",
    Sandbox_DynamicEvents_MinHoursBetweenEvents_tooltip = "One event spawns every this many in-game hours. The scheduler picks a random event type and a random usable location each time. Resets when the server restarts.",

    Sandbox_DynamicEvents_GracePeriodHours = "Grace period (hours)",
    Sandbox_DynamicEvents_GracePeriodHours_tooltip = "In-game hours after a fresh game start before the first event may spawn (the first event then lands one full interval later). Ignored on a restored save.",

    Sandbox_DynamicEvents_SpawnMinDistance = "Min spawn distance from player (tiles)",
    Sandbox_DynamicEvents_SpawnMinDistance_tooltip = "Events never materialise this close to a player, so nothing pops into view. A site further out that is not yet loaded is parked and appears as the chunk streams in. 0 disables the check.",


    Sandbox_DynamicEvents_MaxEventsPerArea = "Max events per area",
    Sandbox_DynamicEvents_MaxEventsPerArea_tooltip = "Optional anti-cluster limit: refuses a new event when this many are already standing within the area radius. 0 disables the limit.",

    Sandbox_DynamicEvents_AreaRadius = "Area radius (tiles)",
    Sandbox_DynamicEvents_AreaRadius_tooltip = "The radius used by the max events per area limit. Ignored when that limit is 0.",

    Sandbox_DynamicEvents_MinDistanceBetweenEvents = "Min distance between events (tiles)",
    Sandbox_DynamicEvents_MinDistanceBetweenEvents_tooltip = "An event will not spawn this close to another event that is still standing. The spot frees up once that event is cleared.",

    Sandbox_DynamicEvents_MinDistanceFromVehicles = "Min distance from vehicles (tiles)",
    Sandbox_DynamicEvents_MinDistanceFromVehicles_tooltip = "An event will not spawn this close to any existing vehicle, which keeps crash sites off vanilla traffic jams and road stories. Higher values cost noticeably more to check.",

    Sandbox_DynamicEvents_CleanupZombies = "Remove event zombies when clearing",
    Sandbox_DynamicEvents_CleanupZombies_tooltip = "When an admin clears an event, also remove the zombies it spawned. Only reaches zombies in loaded chunks; any that wandered far away are left alone.",

    Sandbox_DynamicEvents_Debug = "Debug logging",
    Sandbox_DynamicEvents_Debug_tooltip = "Writes verbose scheduler, spawn and cleanup detail to the server console. Turn this on before reporting a problem.",
}
